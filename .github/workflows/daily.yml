name: Falco Daily Distress Bots

on:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    env:
      NOTION_TOKEN: ${{ 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 }}
      NOTION_DATABASE_ID: ${{ 30b891c048598090addce9b11128c94e }}

      # FalcoFB Tier-1 tightening (copy/paste)
      FALCO_ALLOWED_COUNTIES: Davidson,Williamson,Rutherford,Wilson,Sumner
      FALCO_DTS_MIN: "21"
      FALCO_DTS_MAX: "90"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run bots
        run: python -m src.run_all
